[build]
builder = "nixpacks"

[deploy]
startCommand = "/opt/venv/bin/python download_models.py && /opt/venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
